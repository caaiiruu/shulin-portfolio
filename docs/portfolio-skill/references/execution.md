# Execution Rules
Use GitHub-native data → QA branch → Git blob/tree/commit → PR #3 → GitHub Actions clean checkout → Vercel QA.
Do not require local scratch repo when GitHub-native path works.
Large SSOT: targeted Git Data mutation only.
No rollback / force push.
