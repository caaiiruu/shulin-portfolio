# CTBC Shared Capacity Extension — Minimal Work Order

Single purpose:
Add exactly three generic shared capabilities required by CTBC, then resume CTBC migration.

Approved generic extensions:
1. Featured Design Decision optional `deliveryBoundary`
2. Qualitative Outcomes optional section headline + cards + closing scope statement
3. Structured non-image Evidence groups

Rules:
- no CTBC conditional
- preserve existing output when fields absent
- no duplicate renderer/CSS owner
- regress Voucher / DBS / Booking
- after the three extensions pass, resume the already-approved CTBC content migration in the same round
- do not reopen CTBC content
- do not redesign shared architecture
- Production untouched
