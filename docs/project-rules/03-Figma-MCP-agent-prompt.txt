Use Figma MCP to inspect this Figma node:
[貼 Figma frame link]

Goal:
Implement this section as production-ready Next.js code while preserving the existing portfolio design system.

Hard rules:
1. Do not generate a one-off implementation.
2. First audit existing components in src/components/ui, src/components/project, src/styles/components, and tokens.
3. Reuse existing components whenever possible.
4. If a matching component exists, only extend it with a safe variant.
5. Component owns internal layout, spacing, typography, states.
6. Page file only composes components.
7. Do not edit globals.css unless there is no existing component owner.
8. Do not use inline styles.
9. Do not hardcode reusable spacing, typography, radius, or color values.
10. Use tokens for all reusable values.
11. Preserve Sanity as the source of truth for content and images.
12. Implement only this section, not the whole page.
13. After implementation, run npm run build.
14. Then verify rendered DOM classes and computed styles.
15. If the first fix fails, stop and audit root cause. Do not stack overrides.

Output:
Give me executable terminal commands only.